# Markdown preview flag matrix

| Flag / mode | Default | Effect |
| --- | --- | --- |
| (none) | yes | Slice order = `plan.slices` array order |
| `--chronological` | off | Oldest change first (earliest execution started/ended on slice or any step); no-timestamp slices after; plan order tie-break |

No other flags in v0/v1.
